name: 'lending_analytics_platform'
version: '1.0.0'
config-version: 2

profile: 'lending_analytics_platform'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  lending_analytics_platform:
    staging:
      +materialized: view
    marts:
      +materialized: table
